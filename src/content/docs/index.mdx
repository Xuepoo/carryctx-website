---
title: CarryCtx
description: The intent & management layer for AI coding agents. Durable tasks, agent teams, auditable history — served to every agent over CLI or MCP. Local-first SQLite, fully offline.
template: splash
hero:
  title: |
    The <span class="text-gradient">intent &amp; management</span> layer for your coding agents.
  tagline: CarryCtx durably holds what your agent team intends to do, who does it, and what happened — then hands every agent, commander or subagent, exactly its slice of the picture over CLI or MCP.
  actions:
    - text: Read the Docs
      link: /1-getting-started/1-introduction/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/Xuepoo/carryctx
      icon: external
      variant: secondary
---

import { Card, CardGrid, Steps, Tabs, TabItem, LinkCard, LinkButton } from '@astrojs/starlight/components';
import Reveal from '../../components/Reveal.astro';

<div class="ctx-stats ctx-reveal">
  <div class="ctx-stat">
    <span class="ctx-stat-value">0ms</span>
    <span class="ctx-stat-label">network round-trip</span>
  </div>
  <div class="ctx-stat">
    <span class="ctx-stat-value">SQLite</span>
    <span class="ctx-stat-label">single-file state store</span>
  </div>
  <div class="ctx-stat">
    <span class="ctx-stat-value">∞</span>
    <span class="ctx-stat-label">agents, one source of truth</span>
  </div>
</div>

## Core Features

<p class="ctx-lede ctx-reveal">
  CarryCtx is a local-first, Git-native intent and management layer for AI coding agents and the humans
  working alongside them. It keeps the state chat was never designed to hold — the plan, the roster,
  the record — queryable by any agent with one CLI call.
</p>

<CardGrid stagger>
  <Card title="100% Offline & Local" icon="approve-check-circle">
    All state lives in `.git/carryctx/state.sqlite`. No cloud service, no account, no lock-in — pull the network cable and nothing breaks.
  </Card>
  <Card title="Durable Agent Teams" icon="bars">
    A commander plus role-specialized subagents, persisted like every other fact. Handoffs advance through a state machine with compare-and-swap updates; `team context` reads it all back, read-only.
  </Card>
  <Card title="Dependency-Gated Tasks" icon="list-format">
    Tasks carry status, priority, owner, and dependencies. Work stays `planned` until its dependencies complete, and concurrent claims are arbitrated by the database — not by luck.
  </Card>
  <Card title="Auditable by Default" icon="open-book">
    Every transition lands in an append-only event stream, alongside Git-aware checkpoints, immutable decision records, and full-text search across all of it.
  </Card>
  <Card title="Per-Task Worktree Isolation" icon="code-branch">
    Bind a Git worktree to a task so parallel agents on parallel branches never collide, and context resolves from the directory an agent stands in.
  </Card>
  <Card title="AST Dependency Graph" icon="puzzle">
    Scans imports into a file-level dependency graph, exportable as Mermaid/DOT/ASCII, so agents reason about blast radius instead of hallucinating cross-file assumptions.
  </Card>
  <Card title="Presets, Skills & Personas" icon="setting">
    Install workflow SOPs, coding rules, and agent personas into `.carryctx/` — conventions ship with the repository instead of living in prompts.
  </Card>
  <Card title="Agent Analytics" icon="analytics">
    `carryctx stats` reports sessions, throughput, checkpoints, and blockers per agent — real numbers for tuning your harness, computed entirely locally.
  </Card>
</CardGrid>

## Why not just Markdown notes?

<div class="ctx-reveal">
<CardGrid>
  <Card title="Structured, not prose" icon="list-format">
    Tasks, checkpoints, and decisions are queryable rows, not paragraphs an agent has to re-parse from scratch every session.
  </Card>
  <Card title="Git-native truth" icon="code-branch">
    Branch, HEAD, dirty state, and diffs are captured automatically. Never manually transcribed, never stale.
  </Card>
  <Card title="Built for hand-off" icon="random">
    Any agent, in any worktree, resumes with `carryctx resume`. No chat history to re-read, no context to rebuild.
  </Card>
</CardGrid>
</div>

## How it works

<div class="ctx-reveal">
<Steps>
1. **Initialize once.** `carryctx init` creates a local SQLite store inside your repo's `.git/` directory. Nothing leaves your machine.
2. **Declare the work.** Register your agents, create dependency-linked tasks with priorities, and group them under a team with a commander.
3. **Work normally.** Log `progress`, drop a `checkpoint` before you stop, and let CarryCtx capture the Git diff alongside your notes.
4. **Resume anywhere.** A new window, a new agent, or a teammate runs `carryctx resume` and continues from the same facts you left behind.
5. **Dispatch to specialists.** The commander reads `team context`, narrows it with `--agent-for`, and your harness hands each subagent exactly its slice — while CarryCtx keeps enforcing who owns what.
</Steps>
</div>

## See it in action

<div class="ctx-reveal">
<Tabs>
  <TabItem label="Initialize">
    ```bash
    carryctx init
    carryctx agent register --name my-agent --provider user
    ```
  </TabItem>
  <TabItem label="Work">
    ```bash
    carryctx task create --title "Ship the exporter"
    carryctx task claim CTX-0001
    carryctx progress todo "Wire up the CSV writer"
    ```
  </TabItem>
  <TabItem label="Checkpoint">
    ```bash
    carryctx checkpoint \
      --done "Implemented CSV writer" \
      --remaining "Add streaming support"
    ```
  </TabItem>
  <TabItem label="Resume">
    ```bash
    carryctx resume
    # Task CTX-0001, last checkpoint 12m ago,
    # branch feature/csv-export, 2 files dirty.
    ```
  </TabItem>
  <TabItem label="Team">
    ```bash
    carryctx team create --name payments-squad --commander commander-1
    carryctx team member add payments-squad --agent backend-1 --role backend
    carryctx task team set CTX-0001 --team payments-squad
    carryctx team context payments-squad --agent-for backend-1
    ```
  </TabItem>
  <TabItem label="Serve over MCP">
    ```bash
    carryctx mcp
    # Six tools over stdio: task_manager, progress_tracker,
    # decision_logger, graph_explorer, context_manager,
    # project_admin.
    ```
  </TabItem>
</Tabs>
</div>

## Explore the ecosystem

<div class="ctx-reveal">
<CardGrid>
  <LinkCard title="Quickstart" description="Go from zero to your first tracked task in under a minute." href="/1-getting-started/3-quickstart/" />
  <LinkCard title="CLI Reference" description="Every command, from dependency-aware task lifecycle to worktrees, graphs, and team projections." href="/2-cli-reference/1-project-lifecycle/" />
  <LinkCard title="MCP Integration" description="Six stdio tools: plug CarryCtx into Cursor, Claude Desktop, and any other MCP client." href="/3-ecosystem/2-mcp-server/" />
  <LinkCard title="Core Concepts" description="Project, Agent, Team, Session, Task, Checkpoint: the ideas everything else is built on." href="/1-getting-started/2-concepts/" />
  <LinkCard title="Teams" description="Commanders, role-tagged members, CAS-safe handoffs, and the two read-only team projections." href="/2-cli-reference/10-teams/" />
</CardGrid>
</div>

<div class="ctx-cta ctx-reveal">
  <h2>Ready to run your agents on real project state?</h2>
  <p>Install CarryCtx, run <code>init</code>, and give every agent exactly its slice of the plan.</p>
  <LinkButton href="/1-getting-started/3-quickstart/" icon="right-arrow" variant="primary">Get started</LinkButton>
</div>

<Reveal />
