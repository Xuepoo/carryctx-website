---
title: CarryCtx
description: AI 编码 agent 的意图与管理层——持久保存依赖感知的任务、指挥官与子 agent 团队、可审计的完整历史，并通过 CLI 或 MCP 交给每一位 agent 恰好需要的上下文。本地优先，完全离线。
template: splash
hero:
  title: |
    AI 编码 agent 的<span class="text-gradient">意图与管理层</span>。
  tagline: CarryCtx 持久保存你的 Agent 团队打算做什么、由谁来做、发生过什么——再通过 CLI 或 MCP，把恰好属于它的那一片画面交给每一位 Agent：无论是指挥官还是子 Agent。
  actions:
    - text: 阅读文档
      link: /zh-cn/1-getting-started/1-introduction/
      icon: right-arrow
      variant: primary
    - text: 查看 GitHub
      link: https://github.com/Xuepoo/carryctx
      icon: external
      variant: secondary
---

import { Card, CardGrid, Steps, Tabs, TabItem, LinkCard, LinkButton } from '@astrojs/starlight/components';
import Reveal from '../../../components/Reveal.astro';

<div class="ctx-stats ctx-reveal">
  <div class="ctx-stat">
    <span class="ctx-stat-value">0ms</span>
    <span class="ctx-stat-label">网络往返延迟</span>
  </div>
  <div class="ctx-stat">
    <span class="ctx-stat-value">SQLite</span>
    <span class="ctx-stat-label">单文件状态存储</span>
  </div>
  <div class="ctx-stat">
    <span class="ctx-stat-value">∞</span>
    <span class="ctx-stat-label">多 Agent，同一份事实来源</span>
  </div>
</div>

## 核心特性

<p class="ctx-lede ctx-reveal">
  CarryCtx 是面向 AI 编码 Agent 与人类协作者的、基于 Git 的本地优先意图与管理层。它保存那些聊天记录天生装不下的状态——计划、名册、档案——任何 Agent 用一条 CLI 命令就能查询。
</p>

<CardGrid stagger>
  <Card title="100% 离线 & 本地" icon="approve-check-circle">
    所有状态都存放在 `.git/carryctx/state.sqlite` 中。没有云服务、没有账号、不会被锁定——拔掉网线，一切照常运转。
  </Card>
  <Card title="持久化 Agent 团队" icon="bars">
    一位指挥官加上角色化的子 Agent，像其他事实一样被持久保存。交接经由状态机与 CAS 更新推进；`team context` 以只读方式读回全貌。
  </Card>
  <Card title="依赖门控的任务" icon="list-format">
    任务自带状态、优先级、归属和依赖。依赖未完成，工作就停留在 `planned`；并发认领由数据库仲裁——而不是碰运气。
  </Card>
  <Card title="天然可审计" icon="open-book">
    每一次状态转换都会落入只追加的事件流，旁边是具备 Git 感知的检查点、不可变的决策记录，以及覆盖这一切的全文搜索。
  </Card>
  <Card title="按任务隔离 Worktree" icon="code-branch">
    把 Git Worktree 绑定到任务，让并行分支上的多个 Agent 互不干扰；上下文根据 Agent 所在的目录自动解析。
  </Card>
  <Card title="AST 依赖图谱" icon="puzzle">
    扫描 import 语句构建文件级依赖图谱，可导出为 Mermaid/DOT/ASCII，让 Agent 推算改动波及面，而不是幻觉式地假设跨文件关系。
  </Card>
  <Card title="预设、技能与人格" icon="setting">
    把工作流 SOP、编码规则和 Agent 人格安装进 `.carryctx/`——约定跟着仓库走，而不是散落在 prompt 里。
  </Card>
  <Card title="Agent 数据统计" icon="analytics">
    `carryctx stats` 按 Agent 汇总会话数、产出、检查点和阻塞项——用真实数字调优你的 harness，全程本地计算。
  </Card>
</CardGrid>

## 为什么不直接用 Markdown 笔记？

<div class="ctx-reveal">
<CardGrid>
  <Card title="结构化，而非流水账" icon="list-format">
    任务、快照与决策都是可查询的结构化数据，而不是每次开新会话都要重新阅读的一大段文字。
  </Card>
  <Card title="Git 原生的真相来源" icon="code-branch">
    分支、HEAD、脏工作区状态与 Diff 全部自动采集，无需手动誊写，也不会过期。
  </Card>
  <Card title="为交接而设计" icon="random">
    任何 Agent、在任何 Worktree 中，执行 `carryctx resume` 即可恢复——无需翻阅聊天记录，无需重建上下文。
  </Card>
</CardGrid>
</div>

## 工作原理

<div class="ctx-reveal">
<Steps>
1. **一次初始化。** `carryctx init` 会在你仓库的 `.git/` 目录内创建本地 SQLite 存储——所有数据都不离开你的机器。
2. **把工作声明出来。** 注册 Agent，创建带优先级、带依赖关系的任务，并把它们归入一支有指挥官的团队。
3. **正常工作。** 记录 `progress`，在暂停前保存一个 `checkpoint`，CarryCtx 会连同 Git Diff 一并采集。
4. **随时随地继续。** 新窗口、新 Agent，或是同事，执行 `carryctx resume` 就能从你留下的同一份事实继续推进。
5. **分派给专家。** 指挥官读取 `team context`，用 `--agent-for` 收窄范围，再由你的 harness 把恰好那一片交给对应的子 Agent——而「谁负责什么」始终由 CarryCtx 强制维护。
</Steps>
</div>

## 实际效果

<div class="ctx-reveal">
<Tabs>
  <TabItem label="初始化">
    ```bash
    carryctx init
    carryctx agent register --name my-agent --provider user
    ```
  </TabItem>
  <TabItem label="开始工作">
    ```bash
    carryctx task create --title "上线导出功能"
    carryctx task claim CTX-0001
    carryctx progress todo "接入 CSV 写入器"
    ```
  </TabItem>
  <TabItem label="保存快照">
    ```bash
    carryctx checkpoint \
      --done "实现了 CSV 写入器" \
      --remaining "增加流式写入支持"
    ```
  </TabItem>
  <TabItem label="恢复进度">
    ```bash
    carryctx resume
    # → 任务 CTX-0001，最近一次快照 12 分钟前，
    #   分支 feature/csv-export，2 个文件未提交。
    ```
  </TabItem>
  <TabItem label="团队协作">
    ```bash
    carryctx team create --name payments-squad --commander commander-1
    carryctx team member add payments-squad --agent backend-1 --role backend
    carryctx task team set CTX-0001 --team payments-squad
    carryctx team context payments-squad --agent-for backend-1
    ```
  </TabItem>
  <TabItem label="以 MCP 服务">
    ```bash
    carryctx mcp
    # 六个 stdio 工具：task_manager、progress_tracker、
    # decision_logger、graph_explorer、context_manager、
    # project_admin。
    ```
  </TabItem>
</Tabs>
</div>

## 探索生态

<div class="ctx-reveal">
<CardGrid>
  <LinkCard title="快速开始" description="不到一分钟，从零到追踪你的第一个任务。" href="/zh-cn/1-getting-started/3-quickstart/" />
  <LinkCard title="CLI 参考" description="从依赖感知的任务生命周期，到 Worktree、图谱与团队投影的全部命令。" href="/zh-cn/2-cli-reference/1-project-lifecycle/" />
  <LinkCard title="MCP 集成" description="六个 stdio 工具：把 CarryCtx 直接接入 Cursor、Claude Desktop 等 MCP 客户端。" href="/zh-cn/3-ecosystem/2-mcp-server/" />
  <LinkCard title="核心概念" description="Project、Agent、Team、Session、Task、Checkpoint——理解其余一切的基础概念。" href="/zh-cn/1-getting-started/2-concepts/" />
  <LinkCard title="团队" description="指挥官、角色化成员、CAS 安全的交接，以及两个只读团队投影。" href="/zh-cn/2-cli-reference/10-teams/" />
</CardGrid>
</div>

<div class="ctx-cta ctx-reveal">
  <h2>准备好让 Agent 团队跑在真实的项目状态上了吗？</h2>
  <p>安装 CarryCtx，运行 <code>init</code>，让每一位 Agent 拿到的都是恰好属于自己的那一份计划。</p>
  <LinkButton href="/zh-cn/1-getting-started/3-quickstart/" icon="right-arrow" variant="primary">立即开始</LinkButton>
</div>

<Reveal />
